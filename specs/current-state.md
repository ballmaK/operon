# Current State — Operon

## Overview
Cross-platform (Windows-first) desktop Agent company platform. PRD modularized under `docs/prd/`. Implementation not started.

## Phase
- PRD v0.3 macro confirmed
- Skills installed, Ralph scaffold ready
- Awaiting `/prd-build-loop` to generate active-plan and begin build

## Tech Stack (planned)
- Desktop: Tauri 2 + React
- Sidecar: Node or Rust (TBD at scaffold)
- DB: SQLite (M09)
- Sandbox: Docker Desktop required

## Test Command
`TEST_COMMAND` not configured yet — set when scaffold lands.
